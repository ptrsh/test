# Сервис сбора и анализа отзывов

Сервис для автоматического сбора отзывов из магазинов приложений, их анализа с помощью LLM и генерации метрик.

## 🚀 Быстрый старт с Docker

### Предварительные требования
- Docker и Docker Compose
- Make (опционально, для удобства)

### 1. Клонирование и настройка

```bash
git clone <repository-url>
cd review_service
```

### 2. Настройка окружения

**Для разработки:**
```bash
make dev-setup
# или
cp .env.local .env
```

**Для продакшена:**
```bash
make prod-setup  
# или
cp .env.docker .env
# Затем отредактируйте .env с реальными API ключами
```

### 3. Запуск сервисов

```bash
# Сборка и запуск
make build
make up

# Или одной командой
docker-compose up --build -d
```

### 4. Применение миграций БД

```bash
make migrate
# или
docker-compose exec app alembic upgrade head
```

### 5. Проверка работы

```bash
# Проверка здоровья сервиса
make health
# или
curl http://localhost:5000/api/v1/health

# Тестовый запрос
make test-api
```

## 🛠 Доступные команды (Make)

```bash
make help              # Показать все команды
make build             # Собрать образы
make up                # Запустить сервисы  
make up-with-admin     # Запустить с Adminer для БД
make down              # Остановить сервисы
make logs              # Показать логи
make shell             # Подключиться к контейнеру
make db-shell          # Подключиться к БД
make migrate           # Применить миграции
make test-api          # Тест API
make clean             # Очистить Docker данные
```

## 🌐 Доступные адреса

После запуска доступны:

- **API сервиса:** http://localhost:5000
- **Health check:** http://localhost:5000/api/v1/health  
- **Adminer (БД интерфейс):** http://localhost:8080 (при запуске с `make up-with-admin`)
- **PostgreSQL:** localhost:5432

### Данные для подключения к БД через Adminer:
- **Сервер:** db
- **Пользователь:** reviews_user  
- **Пароль:** reviews_password
- **База данных:** reviews_db

## 📝 Использование API

### Получение отзывов

**POST** `/api/v1/get_reviews`

```bash
curl -X POST http://localhost:5000/api/v1/get_reviews \
  -H "Content-Type: application/json" \
  -d '{
    "stores": [
      {
        "type": "rustore",
        "apps": [
          {
            "app_type": "Mobile Bank",
            "package_name": "com.example.alpha"
          }
        ]
      }
    ]
  }'
```

**Ответ:**
```json
{
  "status": "success",
  "message": "Reviews processed successfully", 
  "stats": {
    "new_reviews": 15,
    "processed_reviews": 23,
    "errors": 0
  }
}
```

### Проверка здоровья

**GET** `/api/v1/health`

```bash
curl http://localhost:5000/api/v1/health
```

## 🔧 Локальная разработка

### Без Docker

1. **Установка зависимостей:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

pip install -r requirements.txt
```

2. **Настройка БД:**
```bash
# Запустить только PostgreSQL через Docker
docker-compose up -d db

# Применить миграции
alembic upgrade head
```

3. **Запуск приложения:**
```bash
cp .env.local .env
python main.py
```

### С Docker для БД

```bash
# Запустить только БД
docker-compose up -d db

# Настроить локальное окружение  
cp .env.local .env

# Запустить приложение локально
python main.py
```

## 📊 Мониторинг и логи

### Просмотр логов

```bash
# Все сервисы
make logs

# Только приложение
make logs-app

# Только БД
make logs-db
```

### Файлы логов

Логи приложения сохраняются в `./logs/app.log` (ротация каждые 10 МБ).

## 🔒 Безопасность

### Переменные окружения для продакшена

Обязательно обновите в `.env`:

```bash
# Реальные API ключи
RUSTORE_CLIENT_ID=your_real_client_id
RUSTORE_CLIENT_SECRET=your_real_client_secret  
LLM_API_KEY=your_real_llm_api_key

# Сильный секретный ключ
SECRET_KEY=your_strong_secret_key_here

# Продакшен режим
FLASK_ENV=production
```

### База данных

Для продакшена рекомендуется:
- Использовать внешнюю БД (не в контейнере)
- Настроить SSL подключение
- Регулярные бэкапы

## 🚨 Решение проблем

### Порты заняты

```bash
# Изменить порты в docker-compose.yml или остановить конфликтующие сервисы
sudo lsof -i :5000
sudo lsof -i :5432
```

### Проблемы с миграциями

```bash
# Сброс и повторная инициализация
make down-full
make up
make migrate
```

### Проблемы с правами

```bash
# На Linux может потребоваться
sudo chown -R $USER:$USER logs/
```

### Очистка Docker

```bash
# Полная очистка
make clean
docker system prune -a --volumes
```

## 📈 Расширение функциональности

### Добавление нового стора

1. Создайте новый клиент в `app/clients/your_store.py`
2. Зарегистрируйте в `ReviewObserver.store_clients`
3. Добавьте переменные окружения

### Добавление новых полей LLM

1. Обновите `LLMAnalysisResult` в `app/models/reviews.py`
2. Создайте миграцию: `make migrate-create MESSAGE="add new llm field"`
3. Примените миграцию: `make migrate`

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи: `make logs`
2. Проверьте здоровье: `make health`  
3. Проверьте подключение к БД: `make db-shell`
4. Очистите Docker данные: `make clean`